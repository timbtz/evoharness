# Untested proposals from branch B3 (s102-48117936 analyst reports + ledger facts)
Absorbed from analyst-stellar_p2-s102-48117936-1/-2/-3. Context: B3 died with ZERO feasible out-of-ball returns — every val-positive return was in-ball (bank_dist ≤ 6e-4), every out-of-ball return collapsed at val. B4 (`stellar_p2-s103-71917443`) confirmed that bank seed #4 also sits on the QI wall (-4.002), making QI-margin-triage a wash, but high-keep truncation (keep=8) produced a feasible in-ball anchor (val 0.6054).

## Proposals (none cleanly evaluated)
- **Interior-escape generation — deeper dilation ladder under a hard margin filter (@30 P1):** after truncation, batch `_dilate(+2e-3, +3.5e-3, +5e-3)` in one `eval_many`; aspect scales ~1/(1+eps), so +5e-3 takes 10.039 → ≈9.99. Select by max L **subject to** `aspect ≤ 9.98`, `log10_qi ≤ −4.005`, `bank_dist ≥ 1.03e-3`; L-key removed from escape selection; fall back to the bank camper if none qualifies. B3 only ever generated the +1.6e-3/+3.2e-3 rungs; B4 added +5e-3/+8e-3 but they failed the QI/feasibility filters.
- **Constraint-margin re-triage of the bank (@30 P2):** stop hardcoding `BANK_I=3` or `BANK_I=4`; read `seed_bank_info()`, skip the 3 tolerance campers AND the QI-wall-campers (e.g. #4 at -4.002), truncate the deepest-QI-margin seed. B4 proved bank #4 has no QI headroom; the search must find a seed with QI strictly < -4.01.
- **Orth-shift on an already-truncated escape (@10 P3):** c0008's directed orthogonal shift failed applied to the FULL bank seed (kept all wall-camping high modes, val −0.27); applying it to a truncated escape might buy distance while shedding those modes. Never tried.
- **Interior polish at visible sigma (@20 P3):** polish from a margin-positive start (margin ≤ 0.003) with sigma start 0.01 / floor 0.003 (never the invisible 0.0012/0.0001 regime), rejecting children whose LF proxies re-enter the headroom band. Precondition: proposal 1 must supply the interior start.
- **Second-basin repair arm, firewalled (@20 P4, @30 P4):** one truncate-and-LF-check probe of the best non-#3/#4 seed, and/or an ~8-eval NAE mp=1 shaped-descent arm — ARCHIVE ONLY, never touching `best_b`/`safe_b` (c0010's unfirewalled NAE fallback returned a bank camper, val −0.0115).
- **Return-gate budget-dry leak fix (@30 P3):** the `ok is None and fm.remaining() <= 0 → trust vlf` fallback returns LF-UNVERIFIED boundaries (the c0006f val −0.0115 mechanism). Track the last boundary that actually passed an LF eval and return that; reserve the LF evals as a hard budget floor the polish loop cannot consume.

## Structural constraint the next branch must design around
**Train-key misalignment:** margin-first returns lose selection — c0007f (0.5574/0.5697) and c0015f (0.5654/0.5757) were rejected against the officially-unsubmittable 0.5789 in-ball camper. An out-of-ball val-feasible boundary carries no novelty penalty, so it needs raw p2 ≥ 0.579 (L ≈ 11.6) to win selection; B2's escape hit val 0.6280, so the level is attainable. Do not submit margin-first candidates that regress train > ~0.02, or selection kills them regardless of merit.

## Verdict
promising / untested — proposals 1+2 (margin-filtered interior escapes from a QI-preserving start) address the exact cause of death of B2, B3, and B4. Log every generated child's eval explicitly so silent gate-filtering and wall-clock starvation stay detectable.
