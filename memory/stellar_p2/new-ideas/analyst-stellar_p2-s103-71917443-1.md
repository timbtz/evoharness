# Analyst notes — stellar_p2-s103-71917443 @ 10 candidates

## What the search is doing

The branch is executing Open direction #1 (margin-interior escapes from bank #4) and its 10 candidates split cleanly into four buckets:

- **2 crashes:** c0001 (`.ravel(8)` TypeError) and c0005 (mode-graft portfolio died before a single graft was evaluated — the mechanism is still *untested*, not refuted).
- **3 bit-exact ties at train 0.5697408497101664** (c0001f, c0002, c0004r2). That number is the raw bank-#4 fallback with the in-ball novelty penalty — i.e. the elaborate escape portfolios never changed the returned boundary. This is the same decoration death spiral as s101/s102, mediated here by the timing guard: `teval()` is a cumulative average that jumps to ~25 s after the first bank-canvas batch, so `reserve()` ≈ 2·2.2·25+5 ≈ 115 s of the 220 s `DEADLINE`. Phase 0 evaluates ~4–6 of the 10-entry portfolio and Phase 1 polish essentially never runs.
- **1 real gain:** c0003 (train 0.6049 / val 0.6054, feasibility 0.0) — the keep=8 truncation of bank #4. It is the branch best but sits at bank_dist 7e-4: in-ball, unsubmittable, carrying ~0.015 of novelty penalty.
- **3 rejections that map walls:** c0002f (QI-margin triage, 0.5724), c0003f (negative R-shift, 0.5711 — completes the refutation of R-shifts in both signs), c0004f (keep∈{8,7,6} deeper truncation ladder: train 0.5776 but **val −0.024** — a textbook vlf landmine that the current 2-finalist LF gate certified anyway, because the gate checks only `_key`, not the ≤0.003 official-feasibility razor).

Mechanism accounting: of the three escape families in the current portfolio (`_truncate`, `_dilate`, `_r_shift`), only truncation has ever produced value, and both dilation rungs (+5e-3/+8e-3) and R-shifts belong to wiki-refuted families (`dilation-ladders.md`, `major-radius-and-frobenius-escapes.md`). The Phase-1 generator spends a coin-flip half of its (rare) evals on a refuted dilation. The `safe` razor tracker (feas ≤ 0.003 AND qi ≤ −4.005 AND aspect ≤ 9.98 AND bd ≥ 1.03e-3) has never populated in 10 candidates — the same permanently-empty-razor failure documented for s102 c0009f.

## Binding problem(s) now

1. **Escape supply is still zero, but the problem has narrowed.** Every positive-val return is in-ball; every out-of-ball attempt val-collapsed or never executed. The concrete new fact is c0003: a *feasible, val-confirmed* anchor at bd 7e-4. The task is no longer "escape from a wall-camping bank seed" — it is "add ≥ 3.3e-4 of max-coefficient distance to a feasible anchor without breaking vlf physics." None of the surviving non-refuted mechanisms in the current code can do that: Gaussian polish drifts too slowly (passive-drift refutation), and dilation/R-shift are dead.
2. **Eval starvation is the mediator, and it is code-inflicted.** On the 11×21 bank canvas an eval costs 12–27 s CPU, so the true budget is ~10–18 evals, not 72 — yet the optimizer plans in wall-clock seconds with a cumulative-average `teval` that over-reserves half the deadline, and it *meters evals on boundaries that are provably unsubmittable* (truncations whose bd could have been read for free via `fm.bank_dist` before eval). There is also a latent kill hazard: `eval_many` on 2 workers accrues CPU at ~2× wall, so 220 s of wall under the guard can breach the 240 s CPU limit.
3. **The selection bar just rose.** With the 0.6049 in-ball camper as branch best, an out-of-ball return now needs val ≳ 0.605 to win selection (Open direction #3, harsher than the 0.579 bar B2 faced). Half-hearted escapes at val 0.57 will be systematically out-selected by the camper, so escape candidates must carry the anchor's L, not restart from scratch.

## Proposals (ranked, concrete)

**P1 — Anchor-first fallback + eval-count budgeting (mechanical; raises the floor of every future candidate).** Rebuild `_truncate(fm.seed_bank(4), 8)` deterministically, evaluate it *first*, and set `fallback_b` to it on success. Every guard-truncated or crashed-late candidate then returns ~0.605 instead of the 0.5697 tie attractor — this single change deletes the plateau that consumed 3 of 10 slots. Replace `teval`/`reserve` wall-clock logic with an eval-count ledger (measure CPU-per-eval from the first batch; plan ~12 evals: 1 anchor, ~8 escape/polish in batches of 2, 2 LF finals; stop by count, not seconds). Delete the dilation and R-shift rungs from the portfolio *and* the Phase-1 coin-flip — both are wiki-refuted and currently eat half the polish evals.

**P2 — Repair c0005's mode-graft as the distance vehicle (the only non-refuted way to cross the 3.3e-4 gap).** Single-coefficient ladders, negative contraction of the zeroed modes, and dilation are all refuted; low-order grafting between bank optima is the task-designated open game and has executed zero times (c0005 crashed on it). Concretely: center-pad bank #1's matrices to the anchor's canvas (per `numpy-shape-bugs.md` — never right-pad), graft its m=1–2, |n|≤2 row envelope onto the c0003 anchor as `anchor + α·(graft − anchor_lowmodes)`, and choose α by *free* pre-screening: sweep α, keep only values where `fm.bank_dist` ≥ 1.1e-3 against every seed and the max per-coefficient delta stays ~1e-3 spread across low-order (vlf-visible) modes. Return gate must enforce the official-feasibility razor — LF violation ≤ 0.003 — or fall back to the anchor; c0004f proves the current `_key`-only LF gate certifies landmines.

**P3 — Proven mechanism × new basin: keep-truncation of bank #3.** The truncation residual (the zeroed high-mode mass) differs per seed, so `_truncate(seed3, k)` may land *outside* the ball where #4's landed at 7e-4 — and bank #3 is the one seed with QI headroom (QI-interior points documented at bd 3.1e-5; #4 has none at −4.002), so its truncations should survive the vlf→official feasibility gap better. Pre-screen `fm.bank_dist(_truncate(seed3, k))` for k ∈ {8, 9, 10} at zero cost; meter only the out-of-ball survivors, LF-razor-gate as in P2. This is distinct from c0002f, which triaged the base via metered QI evals and then escaped with refuted dilation rungs.

**P4 — Free-geometry prefilter on everything metered.** `fm.bank_dist` is unmetered; nothing except the single anchor eval should ever be sent to `fm.eval` with bd < 1.03e-3. Apply the filter to Phase-0 portfolio entries and every Phase-1 mutation before batching. At 12–27 s per eval this converts the current in-ball eval waste (most of the 10-entry portfolio) directly into escape-probe supply — the resource Open direction #1 says the branch is dying without.
