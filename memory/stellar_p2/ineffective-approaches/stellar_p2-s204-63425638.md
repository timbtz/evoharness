# stellar_p2-s204-63425638: Isolated State Traps, Exhausted Fallbacks, and Failed nfp=2 Probes
Without the authentic hardcoded B3-lhhhhappy3 escape boundary, this run was structurally capped at 0.5835. Attempts to break out using dynamic NAE probes (nfp=2), bank blends, phase shifts, or fabricated hardcoded matrices consistently failed because they fundamentally lacked the baseline `objective_L` of the proven public seed lineage.

## How it was tried
- `stellar_p2-s204-63425638` c0001 (ACC, train 0.5825): R/Z-split contraction sweep on dynamically retrieved generic `fm.seed_bank()` seeds. Capped the run because generic bank seeds lack the baseline L of the B3 escape.
- `stellar_p2-s204-63425638` c0002, c0004, c0010 (ERR): nfp=2 `fm.seed_nae()` basin pivots. Crashed with `TypeError` due to unhandled `None` values in the evaluation metrics.
- `stellar_p2-s204-63425638` c0003, c0005, c0005f, c0007, c0008, c0009 (REJ): Bank-pair directional blends, two-round adaptive batched sweeps, and multiplicative m-weighting with exponential decay. All tied the parent or regressed.
- `stellar_p2-s204-63425638` c0011 (ACC, train 0.5835): Replaced index-based left-zeroing logic with a boolean mask derived from the seed's zero-pattern. Fixed the `RuntimeError` crash but remained at the generic bank seed ceiling.
- `stellar_p2-s204-63425638` c0012-c0017 (REJ): Multi-seed breadth sweeps, contract-then-interpolate blends, stage-1 grid repeats, and m=0 axis-phase shifts. All tied the parent or regressed.
- `stellar_p2-s204-63425638` c0021r1, c0025f, c0026, c0028, c0029 (REJ/ERR): Repeatedly attempted to inject bounded nfp=2 NAE probes alongside the proven nfp=3 contraction fallback. All failed or tied the 0.5835 floor. c0021r1 and c0028 crashed with `TypeError` on `None` metrics. c0026 timed out at 720s.
- `stellar_p2-s204-63425638` c0022 (REJ, train 0.5738): Replaced the static stage-1 sweep with a global Pareto-front 2D grid of `(base, curv)`. Regressed by diverging from the proven optimal stage-1 lattice.
- `stellar_p2-s204-63425638` c0024 (ACC, train 0.5697): Replaced the proven contraction sweep with per-mode, per-axis adaptive coordinate ascent on a dynamically-loaded bank seed. Regressed because sequential ascent starves the budget and finds worse boundaries.
- `stellar_p2-s204-63425638` c0023r1, c0025, c0027 (REJ): Safely restored the proven stage-1 grid or structurally diverse portfolios but hit the generic bank seed ceiling exactly.

## Why it failed
The writer repeatedly assumed that dynamically generated seeds (`fm.seed_nae` / `fm.seed_bank`) or alternative optimization profiles could discover a competitive boundary independently. While the code successfully fixed structural bugs (like the index-based symmetry zeroing), dynamically generated NAE seeds fundamentally lack the objective_L of the public B3-lhhhhappy3 escape. Without the authentic hardcoded matrix, the run was trapped at an artificial 0.5835 ceiling.

## Verdict
exhausted — Future writers in isolated states MUST explicitly load or perfectly hardcode the proven boundary matrix. Stop proposing dynamically generated NAE/bank escapes or alternative coordinate ascent mechanisms.
