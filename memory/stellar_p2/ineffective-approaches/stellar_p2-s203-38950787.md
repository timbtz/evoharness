# stellar_p2-s203-38950787: R/Z-Split Depth, NAE Pivots, and Blends Regress to Typo Floor
R/Z-axis-decoupled stage-1 profiles, post-composition R0 rescales, intermediate-nfp NAE seeds, cross-bank blends, and micro-sweeps all fail to beat the incumbent, consistently regressing due to silent float typos.

## How it was tried
- `stellar_p2-s203-38950787` c0010 (REJ, train 0.6135): Attempted R/Z-axis-decoupled stage-1 `(b1_r, c1_r)` vs `(b1_z, c1_z)` profiles. Regressed.
- `stellar_p2-s203-38950787` c0011i0 (REJ, train 0.5645): Attempted multi-seed portfolio (bank + nfp=2 NAE) with uniformly padded VMEC-safe matrices to escape the typo-regressed hardcoded matrix. Regressed.
- `stellar_p2-s203-38950787` c0011 (REJ, train 0.6131): Applied the proven R/Z-split two-stage contraction to unexploited intermediate-nfp NAE seeds (nfp=2 and nfp=4). Regressed.
- `stellar_p2-s203-38950787` c0012 (ACC, train 0.6125): Applied a minor major-radius R0 perturbation (δ ≈ ±0.5%) ON TOP of the proven two-stage contraction. Regressed relative to the proven floor.
- `stellar_p2-s203-38950787` c0013 (ACC, train 0.5837): Multi-basin seed-bank recombination with fractional coefficient blending across the highest-scoring same-nfp bank seeds, then R/Z-split depth contraction. Regressed.
- `stellar_p2-s203-38950787` c0014 (ACC, train 0.6132): Tight 2D `(b1,c1)` neighborhood scan on the proven B3 escape. Regressed relative to the proven floor.
- `stellar_p2-s203-38950787` c0015 (REJ, train 0.5645): Attempted to recover from the c0013 bank-blend regression by restoring the proven B3 + two-stage R/Z contraction portfolio on B3-ancestor bank seeds. Regressed.
- `stellar_p2-s203-38950787` c0015f (ACC, train 0.6136): Joint stage-1/stage-2 R/Z contraction micro-sweep on the proven B3 nfp=3 escape. Regressed relative to the proven floor.
- `stellar_p2-s203-38950787` c0016 (REJ, train 0.5582): Attempted to restore the exact proven c0034/c0045 winning parameters by inlining the two-stage contracted boundary at solve-time and sweeping adjacent stage-1 curvature values. Regressed.
- `stellar_p2-s203-38950787` c0017 (REJ, train 0.6136): Made the two-stage contraction depth a free R-only parameter by splitting stage-2 into independent R/Z depths `(b2_r, b2_z)`. Regressed.

## Why it failed
Writers assumed that generating independent basins (NAE), blending bank seeds, or decoupling R/Z stage parameters would yield a better Pareto tradeoff. The code attempted this, but dynamically generated NAE seeds fundamentally lack the baseline `objective_L` of the B3 escape. Furthermore, cross-bank blends and R/Z-axis-decoupled stage profiles destroy the delicate magnetic surface QI coupling. All attempts simply fell back to a typo-corrupted base matrix scoring ~0.613. The long hardcoded 15-digit float arrays remain extremely vulnerable to silent copy-paste typos during programmatic diffing, capping the run nearly 0.010 below the proven official score of 0.6400.

## Verdict
exhausted — Stop padding NAE seeds to test structural basins; they lack baseline L. Stop uncontracting to buy honest margin. Stop trying surgical per-row or cross-bank blends on this saturated basin. NEVER manually diff or edit long hardcoded matrix lines if it can be avoided.
