# Deviations from the briefing (one line each)

- Cheap role model = `glm-4.5-air` ($0.2/$1.1 per 1M): cheapest z.ai model that reliably emits fenced code; strong = `glm-5.2`.
- Wiki knowledge: Mode A "inject" (index + top-2 pages by keyword overlap) is the default; Mode B "tool" (read_wiki, ≤2 reads) is implemented and selected via `Config.wiki_mode`.
- `Task` grew a `description` attribute (the prompt header) on top of the §3 protocol — every task needs it, the loop stays generic.
- Objective scalars: `quality_per_dollar` = (score − seed score)/max(usd, 0.01) at gate time; `time_capped` ≡ `quality` because the budget guard already enforces `max_seconds` for every run.
- Candidate ids are deterministic (`c0000` seed, `c{gen:04d}`, migrants `{id}m{t}`) so golden test 6 holds across processes.
- Binpacking private split: Zenodo 14162744 was inspected but rejected (capacity-150 / 250-item protocol mismatches our capacity-100 / 5000-item setup) → generated OOD instances instead (2× Weibull(1.5)·30, 2× uniform 20..70).
- PyVRP added as a ninth reference repo (user request): its Statistics/Result split maps to our ledger JSONL (write side) + `experiments/report.py` (read side, incl. CSV export à la `Statistics.to_csv`).
- GLM thinking mode disabled on every call (`thinking: {type: disabled}`): with it on, glm-5.2 spent 2.6k–16k+ completion tokens per call and sometimes never emitted content before the cap (parse errors at $0.07/call); with it off, the same prompt yields a clean code block in ~240 tokens. Evolution wants many cheap samples, not per-call deliberation.
- `parse_code` accepts a final unterminated ```python block (max_tokens truncation) — a syntactically broken tail just scores −inf in the sandbox; writer `max_tokens` raised 4096 → 16384.
- Fourth task `matmul_c` added (user request, 2026-07-18): C is now a first-class artifact language — `core/sandbox.run_c` compiles with fixed flags (gcc -O3 -march=native) and runs the binary in the same unshare/rlimit jail; `parse_code` + loop prompts made language-agnostic. Score = geometric-mean speedup vs the naive kernel timed in the same binary (cancels VPS load noise); correctness gated vs naive at 1e-7·n. Verified: seed 1.02×, hand ikj 11.9×.
- Fifth task `cvrp` (anytime CVRP, 2026-07-18): the evolved artifact is an entire module exporting `solve(coords, dist, demand, capacity, deadline, compile_c)`; scoring runs in a dedicated docker container (`tasks/cvrp/Dockerfile.eval`, `--network none --cpus 1 --cpuset-cpus 3 --memory 1g`) with automatic fallback to the unshare sandbox (`core/sandbox.run_python_docker` + `docker_image_ready`).
- cvrp scoring is HOST-side: the sandbox returns routes; `task.py` re-validates and re-prices them against its own distance matrices — a candidate printing forged JSON (or `os._exit`ing after it) scores -inf because no valid routes exist. Per-instance deadline enforced post-hoc (+1 s grace) with a harness-held `time.monotonic` reference; a non-returning solve is hard-killed by the sandbox timeout (n·budget + 15 s).
- cvrp task.py is 213 LOC (> the usual 150; the briefing pre-authorized ~250) and the seed lives in `tasks/cvrp/seed.py` (224 LOC, read as text) — inlining a 200-line seed string would have blown task.py past 400.
- cvrp A/B/P instances are fetched from the galgos CVRPLIB mirror by numeric id (the relaunched site has no name-based URLs; ids pinned in fetch.py), X instances from PyVRP/Instances by name.
- cvrp adversarial-review fixes (opus subagent): `run_harness` hardened against non-dict/malformed payloads (all tasks benefit); cvrp solutions matched positionally with instance NAMES withheld from the sandbox (denies memorized-BKS lookup keys); host-side route-length bound + empty-route filter; `compile_c` temp dirs cleaned up; docker runner gained `--cap-drop ALL`, `no-new-privileges`, `--memory-swap` cap and a wedged-daemon kill path. Accepted residuals: instances remain fingerprintable by coords/n; per-instance +1 s grace can stack but total wall is capped host-side at sum(budgets)+8 s; sandbox stdout is unbounded (PoC threat model, pre-existing for all tasks).
- cvrp new render kind "routes" (multi-polyline) added to web/index.html — the existing "tour" renderer draws a single closed tour and cannot show multiple depot-anchored routes.
- Docker: single `api` service (runs execute as threads inside the API process — a separate worker service would duplicate state for no isolation gain at this scale); inside containers without user namespaces the sandbox auto-falls back from `unshare -rn` to the socket-stub preamble.
- Compile repair is mandatory baseline, not a sixth axis (user decision, 2026-07-20): a candidate whose error is a C-toolchain failure (`compile failed` from cvrp's `compile_c`, or raw gcc `.c:…error:` diagnostics from `run_c`) is repaired in-conversation — the same writer gets the compiler stderr as an immediate follow-up turn, up to `COMPILE_REPAIRS = 2` extra calls per generation (`core/loop.py`). Failed attempts are still ledgered as rejected candidates (`c0007`, then `c0007r1`, `c0007r2`); the last attempt becomes the generation's candidate. Rationale: "it compiles" is a floor every optimizer variant should share, so leaving it as a switch would confound the 5-axis screening; runtime/validation errors remain ordinary next-generation feedback (only the LAST candidate's error enters the next prompt, capped at 800 chars — errors never accumulate).
- Run continuation (user request, 2026-07-20): `Config.resume_from = <run_id>` seeds c0000 with that run's best accepted candidate (scanned from its ledger, train-score max, latest on ties; fallback `best.py`) instead of `task.seed_code()`. Task must match the resumed run's; validated fail-fast in `POST /api/runs` (422) so a bad request never creates a dead run dir. Frontend: "⟳ continue" button on done runs in the past-runs strip — starts a new run with the current panel config + `resume_from`, task forced to the resumed run's. A continuation is a NEW run (own dir/ledger/budget) referencing its ancestor via config + c0000 meta, keeping ledgers append-only and single-run.
- Plateau fixes (run-analysis 2026-07-20, binpacking-s0-41921694 / -41655420): (a) gates accept exact score TIES when novelty > 0 (lateral drift across plateaus — most binpacking mutations are argmax-equivalent to their parent and previously froze the search); (b) Greedy11 and Islands rank equal scores by newest gen so accepted ties actually become the incumbent; (c) Islands seeds the root candidate as island "all" (bugfix: it was pinned to island 0, leaving islands 2-3 empty → IndexError in rng.choices every visit, half of all generations skipped) and never prunes it; prune keeps newest-per-unique-score.
- Reflections learn from REJECTED candidates too (they carry all the signal under strict gates; previously reflections required >= 2 accepted candidates and never fired in runs where nothing was accepted — 0 reflect calls in 32). Identical (worse, better) pairs reuse the cached reflection instead of re-billing two calls per generation.
- Tie feedback: when a candidate scores exactly its parent's score, the next prompt says so explicitly (order-preserving decoration, no decision changed) — cheapest and most actionable signal the harness can emit.
- Binpacking wiki: heuristic-families.md now leads with the ordering rule (argmax invariance: monotone transforms ARE best-fit; a term must have slope > ~1 per unit gap, or a discrete jump > 1, to flip any placement) and the composition template uses magnitudes that actually reorder — the old template was bit-identical to best-fit and funneled writers into a score-neutral equivalence class.
- CVRP splits restructured (train saturation, run cvrp-s1-40028152): seed hit gap 0.0 on all five small A/B/P train instances → gate signal saturated at the max. Now train = X-n101/110/125 @5s, val = X-n153 @5s, private = X-n200/251/303 @6s; the small A/B/P set (still optimal for the seed in <3s) is the public progress-report split.
- Staged search (user design, 2026-07-20, third `search` option): stagnation-driven pipeline — climb the incumbent; after 50 generations without a new GLOBAL best, demand a full redesign and ADOPT it as the new branch root even if it scores worse (it gets its own 50-gen budget); operator directives (simplify/combine/wildcard) rotate in every 25 barren generations. Loop hooks: search may expose prompt_sections() (directive injection) and adopt(cand) (gate bypass, tried before the gate). Branch ids in cand.meta; pool.best() ignores branches so the run's result is always the global best (elitism). Not part of the 2-option screening grid; screening keeps greedy|islands.
- Live user steering: a run_dir/HINT file's text is injected each generation as a "Directive from the user" prompt section (edit/delete anytime, like STOP). Applies to every config.
- Per-instance feedback: parent and rejected-attempt sections now carry gaps_pct when the task reports it (cvrp); plain worse-and-rejected attempts get an explicit rejection note (previously silent).
- Noise-aware gating: tasks may declare `noise = {split: eps}`; gates re-evaluate a candidate whose margin vs parent is within eps (median of 3) before deciding. cvrp: train 0.02, val 0.05 gap-pct pts (measured: same-code private evals varied 0.628% vs 0.704%).
- Reasoning tracing (user decision, 2026-07-22): `llm_call` events now carry the full response `text`; every candidate records the writer's `Idea:` line (`meta.idea`, contract added to SYSTEM + trailing instruction) and its prose preamble (`meta.reasoning`, code fences excluded); Reflections logs a `reflection` event (pair, short, long) whenever a new pair fires. Rationale: run analysis of cvrp-s3 was only possible via code diffs — "why" was unrecoverable (parse_code discarded the preamble; reflections lived only in memory). JSONL, so no migration; report.py/web ignore unknown fields/types.
- Memory wiki (user design, 2026-07-22, third `feedback` option "memory", axes/memory.py): run-scoped Karpathy-LLM-wiki at run_dir/memory — index.md as the library (one line per page), SCHEMA.md as the maintainer instructions, four ShinkaEvolve-scratchpad section folders (successful-patterns / ineffective-approaches / implementation-insights / performance-analysis), one file per distinct pattern. Writers READ ONLY: index injected each generation + read_memory/grep_memory tools (loop now merges tools from knowledge AND feedback axes). A separate reviewer agent (feedback model, temp reflect, role "reviewer") maintains the wiki: fires every 20 candidates since last review OR on a new global best, sees only the unreviewed stretch (cap 50: id, accepted, score, error, novelty, idea, reasoning), rewrites pages via `=== FILE: path ===` blocks (path-validated to the four sections + index.md), ledgered as `memory_review`. resume_from copies the ancestor run's memory dir — cross-run memory accumulates. Not in the screening grid; precedents: ShinkaEvolve summarizer, KernelEvolve filesystem KB (arXiv 2512.23236), Karpathy llm-wiki, GEPA (reflective verbal learning beats scalar-only signals).
- Memory wiki correction (user decision, 2026-07-22, same day): NOT per-run — ONE shared wiki per task at `<repo>/memory/<task>/` (e.g. memory/cvrp/); every feedback=memory run reads and extends the same memory, resume-copying removed. Per-run traceability stays in the ledger (memory_review events; the reviewer's full output is in llm_call.text). SCHEMA.md now mandates conservatism: strengthen existing pages ("tried N times / variants — score-neutral") instead of adding prose when a stretch merely re-tried known approaches; record HOW an approach was implemented, not attempt narratives; an update touching nothing but index.md is a valid review. Known accepted residual: two concurrent runs on the same task can interleave reviews (last-writer-wins per file) — fine at PoC scale.
- Memory reviewer v2 (user decision, 2026-07-22, after cvrp-s3-45465035 A/B — private gap 0.581% vs score_only's 0.748%): SCHEMA.md now enforces a COVERAGE rule (every distinct approach in the reviewed stretch appears exactly once — own page or labeled bullet; parse/compile-dead ideas go to new-ideas/, never dropped) with a mandatory markdown page format (# title / claim / ## How it was tried, bullet per variant with ids / ## Why it worked-failed grounded in code / ## Verdict), replacing the earlier be-conservative wording that produced lossy 200-1000-token reviews. Reviewer digests now include each candidate's CODE (full, or unified diff vs parent when >4k chars, capped 6k) — judging attempts by the writer's self-description misclassified c0027 (kernel-identical overlay labeled "algorithmic replacement"), and the injected error redirected ~1/3 of the run's candidates into a refuted direction; reasoning cap raised 700→2000 chars, reviewer max_tokens 8192→32768. Sixth wiki section new-ideas/ added (SECTIONS + path validation + scaffold). memory/cvrp enriched from the run's full traced ledger and merged with the pre-tracing evidence. Known open items: parse_error generations still charge the slot without retry; eval-noise-aware gate margins may need widening (semantic no-op c0017 spanned 0.09 train / 0.14 val).
- Optimizer v2 (Fable 5 design pass, 2026-07-22, informed by Rekursiv AI's ARC/Sudoku posts + in-flight evidence from cvrp-s5-54146615): (a) Prediction contract — writers emit a falsifiable `Prediction:` line after `Idea:` (parse_prediction, meta.prediction); the memory reviewer sees prediction vs outcome and must record refuted mechanisms (Rekursiv: prediction-gated experiments; their Analyst kill-check is deferred — our eval is seconds, not GPU-days, so pre-eval critique buys little). (b) Anti-re-roll digest — feedback=memory injects the last 10 attempts (id, accepted, train/val, Idea line) with an explicit do-not-re-propose instruction; evidence: 12/20 candidates in cvrp-s5 re-rolled the wiki's top open direction between reviews (wiki staleness window). (c) Reward alignment — run-end best, resume_from, and run_end summary now select by mean(train, val) instead of train-only: HoldoutGate admits val-improvers (e.g. c0006 val -0.052→-0.024 with train regression) that train-only selection could never return; parent selection stays train-only so val remains a genuine holdout. (d) Eval widened — train += X-n186-k15, val += X-n176-k26 (BKS from .sol): train was n<=125 while private is n=200-303, so large-n-only improvements were invisible; val noise drops ~sqrt(2) with 2 instances. Eval cost +10s/candidate. (e) Staged STALL 50→30, OP_EVERY 25→15 (~110 candidates per $10 run; 50 never fired at $4). (f) tool_call ledger events (name, args, result size) — writer tool usage was previously only visible as empty-text llm_calls. (g) experiments/selection_gap.py — Rekursiv's generation-vs-selection diagnostic; on past runs gap ≈ 0.005 (selection fine, generation is the bottleneck → diversity investment, confirming (b)).
- Post-run fixes (cvrp-s5-54146615 post-mortem, 2026-07-23): (a) cvrp noise widened to 0.10 train / 0.15 val — semantic no-op c0017 swung train -0.0906->-0.0072 (bimodal X-n125 55690/55551 attractor, wall-clock-coupled SA); the old 0.02 eps let lucky rolls become incumbents unchallenged. (b) run-end private eval = median of 3 (single private evals spread 0.1-0.18; a transient sandbox failure also must not report -inf for a working candidate — the run's own final eval died to a pruned docker image). (c) cvrp _runner invalidates the docker ready-cache and rebuilds once on "Unable to find image" (9 candidates got false -inf mid-run from an externally pruned image). (d) parse_error joins compile errors in the in-conversation repair loop (12/110 generations died codeless). (e) memory reviewer gained a `=== DELETE: path ===` directive (copy-moves left contradictory duplicates: new-ideas said "never evaluated" while ineffective-approaches said "refuted" for the same page). (f) memory/cvrp editorial pass: ineffective-approaches regrouped into c-side/ and python-side/ subfolders, duplicate deleted, fabricated run id fixed, index rebuilt with multi-eval private medians (c0035 0.636%, c0062 0.730% — the $10 run's train "breakthrough" did not transfer to private).
- Regime-conditional evaluation (2026-07-23, after cvrp-s11/s13 $3 runs): head-to-head evals showed generalization tracks customers-per-route (n/k), not size — c0024 (multi-restart+segment-shift) beat c0035 by 2-3x on long-route val instances (X-n176/X-n214) yet lost on short-route private ones (X-n200-k36/X-n251-k28), while slightly winning X-n303-k21 (14.4 c/r). val rebuilt to match private's n/k mix: X-n153-k22 (7.0) + X-n228-k23 (9.9) + X-n242-k48 (5.0), replacing X-n176/X-n214 (BKS 25742/82751 from .sol). Wiki: performance-analysis/instance-structure.md (evidence table), new-ideas/regime-dispatcher.md (dispatch polish strategy by estimated n/k = ceil(total_demand/capacity); both blocks proven in different regimes), multi-restart page verdict amended regime-conditional. Runs: cvrp-s11-66566581 ($3, v2, best c0002 multi-restart, private 0.749) and cvrp-s13-71671014 ($3, best c0024 segment-shift, private 0.720); anti-re-roll digest verified (0 duplicate ideas vs 12/20 pre-v2).
- Run 4 result (cvrp-s17-78412700, $3, k-matched val, resume c0024): best c0020 = regime-dispatcher (c0001, wiki new-ideas pickup on gen 1) + cheap forward-KNN inter-route Or-opt pre-filter. Private 6-eval median 0.639 vs c0035 0.658 — first candidate to reach champion parity from a different mechanism family; X-n200 0.15/0.19 twice (never seen below 0.44 before), one 0.98 outlier (variance = open problem). Second regime flip recorded: cheap inter-route Or-opt reopened after 18 refutations of heavy variants under the old val. Wiki updated (inter-route page amended, index Current best now 6-eval medians).
- Two-budget Pareto scoring (user request, 2026-07-23): cvrp gains split "val30" (same val instances @30s/instance) declared via task.extra_splits — evaluated ONLY for accepted candidates + seed (~90s each, a few per run), never for the full population. Writers see the parent's 30s-val score in the prompt; run_end reports a second champion best30 (best val30, private median-of-3, best30.py) when it differs from the 6s-combined best. Rationale: PyVRP crossover measurement (6s: ours ~0.66% vs PyVRP ~2.1%; 60s: PyVRP ~0.49%) shows budget-regime specialization; the 30s probe makes anytime-generalization visible and preserves both Pareto ends for resume. Time limit itself is NOT a KPI to minimize — it is part of the task definition; the KPI is route quality AT given budgets (two-point approximation of the anytime/primal-integral curve).
- Web knowledge axis (2026-07-23, user decision): knowledge option 3 `web` = a research analyst with real internet access, on the REVIEWER side — NOT writer-side search tools (rejected: adds tool rounds to every generation, pollutes the writer contract). axes/research.py Researcher fires every 30 candidates or on 15 consecutive rejects (15-candidate cooldown, max 3 sessions/run) and runs a deterministic map-reduce that protects context instead of one giant agentic session: (1) question call frames 2-3 cross-domain research questions from the memory index's open directions; (2) each question gets a fresh-context tool session (web_search + fetch_url, 6 rounds); (3) synthesis call writes 1-2 new-ideas/web-*.md pages (namespace-enforced: only new-ideas/, only web-* names) with mechanism translated to the task, falsifiable prediction, and ## Sources URLs — then appends them to index.md so writers see them immediately. Provenance chain: Idea line -> page -> URL; ledger `research` events carry questions/files/sources, tool calls ledgered with args. Rules: IDEAS never code (no GPL derivation). Requires feedback=memory (Config enforces — pages are delivered via the memory wiki; fewer combinations, memory already won its A/B). Backend: DuckDuckGo HTML POST — z.ai's web_search is NOT on the coding-plan subscription (probed: standalone /paas/v4/web_search -> error 1113 "no resource package"; in-chat web_search tool on coding endpoint -> request hangs; control without tool -> 200 in 8s). llm.chat gained a `rounds` param (default 3, unchanged). A/B design: memory vs memory+web, same seed/resume; KPI = accepted lineage tracing to a web-*.md page. Golden test 12.
- Web search backend amendment (2026-07-23, after user pointed at the MCP variant): z.ai DOES expose web search over MCP streamable HTTP (`POST /api/mcp/web_search/mcp`, tools webSearchSogou/Quark/Pro/Std) and our coding key passes the protocol handshake — but every tools/call returns 429 "no resource package", and the in-chat `web_search` tool on the coding chat endpoint is silently ignored (no search_result field, model answers from weights; verified with an unanswerable live-data query, 24 prompt tokens). Conclusion: the account's web-search resource pack is inactive or belongs to another key — user to check the z.ai console. research.py web_search() now tries `_zai_search()` (MCP webSearchPro) first and falls back to DuckDuckGo on any error, so activating the pack upgrades runs with zero code changes.
- Tool-loop attractor fix (2026-07-23, live during cvrp-s31-5170619): from ~c0021 every generation died parse_error — the writer burned both memory-tool rounds reading pages, then on the silently tool-less final round emitted a "let me check..." fragment (18-121 completion tokens, no code block) and stopped, mid-run evidence: 12 codeless deaths in ~35 generations. Two fixes: (a) live — HINT file in the run dir (the injected user-directive channel) ordering one round of reads then the complete Idea/Prediction/code response; (b) permanent — llm.chat now appends an explicit "Tool rounds are exhausted — produce your complete final answer now" user message when entering the final round after tool use, instead of silently withholding tools (activates on next server restart).
- stellar_p2 task (2026-07-23, Briefing MS0-MS2): ConStellaration P2 wired as tasks/stellar_p2. Evolved artifact = optimizer program `solve(fm, rng) -> boundary dict`; fm = in-container metered forward-model handle (eval/eval_many/score/remaining/seed_nae/seed_ellipse), budget 72 evals AND 240s CPU (both enforced in-template, host kill at cpu_budget+120s slack). Anti-forging: train score (shaped, in-container re-eval of the RETURNED boundary) is selection-only; val/public/private re-score only the cached returned boundary in a clean-room container run without candidate code; private = official SimpleToBuildQIStellarator().evaluate() = the only truth. Score shaping DEVIATION from briefing: shaped = P2 score if feasible else -max_normalized_violation (briefing's "score=0 if infeasible" gives the gate zero gradient below the feasibility wall — and the wall is far: official ALM baseline needed 34h x 96 vCPU to cross it, so the MS1 golden test asserts feasibility IMPROVEMENT (seed: raw NAE -0.95..-1.08 -> -0.665 in 72 evals), not feasibility). Splits: train=very_low_fidelity+QI (no turbulent, ~1.4s), val=low_fidelity+QI (~2.2s), public=vlf, private=official (~64-128s). Deterministic (max_threads=1, fixed rng seed 0, verified bit-identical double-eval + across worker counts) -> task.noise={}, and run-end private eval is now 1 (+retry only on transient -inf) for noise-free tasks instead of median-of-3. Boundary archive memory/stellar_p2/archive.jsonl: every train/val/private eval archives frontier boundaries (shaped >= -0.10 OR beats archive best; dedupe on nfp+coeffs rounded 1e-4); experiments/submit_export.py re-verifies top entries officially and writes leaderboard-ready JSON. Docker image evoharness-stellar-eval (python 3.12 + constellaration==0.2.6 pinned + nevergrad, single-thread env baked). Wiki bootstrapped by hand (memory/stellar_p2/: ALM tricks, fidelity dial, seed baseline, 5 new-ideas starters, inner-update-rule refutation).
- In-container batch parallelism (2026-07-23, user request re: VPS parallelism): box = 4 vCPU / 7.5GB -> concurrent runs are OOM-risky (2 jax containers alone ~6GB+); the useful parallelism is INSIDE the train container: fm.eval_many(list) fans out over a 2-worker fork pool (forked pre-jax-init — fork after XLA init deadlocks; workers pay their own ~9s one-time JIT), container gets --cpus 2 (run_python_docker gained a cpus param) and 4GB. Measured on the identical deterministic 72-eval seed trajectory: 328s sequential -> 242s with 2 workers (1.36x; stragglers dominate pairs). Same final score across worker counts = batch order-independence verified. Side benefit: population/batch strategies become natural for evolved optimizers (documented in description.md + wiki).
- Carried §6 bug fixes (2026-07-23): (1) memory.py index guard — after every review, on-disk pages missing from index.md are re-appended with a "(restored by index guard)" line and ledgered in memory_review.index_restored (s37 review silently dropped 10 pages). (2) already fixed in v2 — tool_call events carry name/args. (3) gate tie-band — an exact tie on the gated split now also requires train >= parent-train - train_noise (s37: val-ties admitted train -0.59 regressions). (4) missing `Idea:` line = contract violation "missing_idea": not evaluated (no simulator time wasted), one in-conversation repair round like compile errors. (5) reviewer prompt now carries a run-id whitelist (existing runs/ dirs, last 40) against fabricated citations. test_compile_repair_loop mock updated to emit Idea: lines (contract-conformant).
- Fable refiner (user experiment, 2026-07-23 evening): new Config field `refiner` (claude model id, default None) — after each writer candidate is evaluated, axes/refiner.py ClaudeRefiner runs a HEADLESS `claude -p --disallowedTools "*" --strict-mcp-config` session (prompt-only, no tools/web) that sees: task description, the full memory wiki, the incumbent parent + score, the last-10 attempt digest, and the just-evaluated candidate (code, idea, scores, metrics, error). It outputs Idea/Prediction/code; the v2 is evaluated and gated EXACTLY like any candidate (id "cNNNNf", parent = the pool parent so gate comparisons stay honest even when the refined-from candidate was rejected; meta.refined_from records provenance). Ledgered as llm_call role "refiner" (usd null — Claude bills separately, z.ai guard untouched; bounded by generations). MemoryWiki gained note(cand, pool) with digest-dedupe so both the writer candidate and the v2 reach the reviewer (previously only `last` per gen was digested). Known accepted risk (user explicitly ok'd): Fable's weights may contain leaderboard knowledge for this public benchmark. Eval-budget deviation for the same campaign: STELLAR_TRAIN_OVERRIDES env var patches _TRAIN (first use: max_evals 160, cpu_budget 480s = ~2.2x depth per candidate; simulator time is the idle box's, LLM cost is per-generation, so deeper evals raise physics-per-dollar).
- Seed bank from public submissions (user decision 2026-07-24, after the refiner campaign confirmed the QI wall is uncrossable at candidate eval scale — best shaped -0.42526, official 0.0): tasks/stellar_p2/seed_bank.json = 12 unique feasible P2 boundaries from the benchmark's public per-submission results dataset (cached rows2.json from the 2026-07-23 research session; full provenance per seed), exposed to candidates as fm.seed_bank(i)/fm.seed_bank_info() (unmetered). vlf validation: top-7 hold 0.60-0.62 at train fidelity (official 0.61-0.64, resolution gap 0.01-0.02); 3 seeds flip infeasible at vlf (tolerance camping at 0.007-0.011 — real examples of the paper's failure mode). High-mode bank boundaries cost 12-27s/eval → _SLACK 120→240 (first bank-seed eval hit the 600s host timeout). Seed optimizer v2: triage top-4 bank seeds, gentle polish (sigma 0.008, no inflation) on the best. THE BAR MOVES: novelty = beating the bank max 0.6361 official; returning/perturbing a bank boundary is worthless and submit_export now reports nearest-bank-seed distance and refuses near-copies (< 1e-3 max coefficient distance). Claim framing everywhere: "refined from public submissions", never "from scratch".

## 2026-07-24 novelty shaping + DAG campaign
Provenance audit showed every elite archive entry within 3.3e-4 of davidkh's
public submission (export guard min distance 1e-3) — locally optimal
micro-polish, but unsubmittable and not ours. Decision: bank_distance() in
tasks/stellar_p2/task.py (the export guard's max-coeff padded metric) is now
computed for every evaluated boundary; feasible train/val scores inside the
1e-3 ball pay a linear penalty up to 0.05 (private/official never shaped;
metrics carry bank_dist / novelty_penalty / submittable). Harness additions for
the DAG branch-and-merge campaign (experiments/dag_campaign.py): Config
stall_stop / review_every / analyst+analyst_every (axes/analyst.py, headless
Fable in-run analysis every 10 cands -> new-ideas/analyst-*.md) / merge_from
(Approach-B injection for branch merges) / resume_from "file:<path>"; refiner
model per-run (campaign: claude-opus-4-8). Success bar: official > 0.6361 AND
bank_dist >= 1e-3.

## 2026-07-25 boundary-report leak fix + scale-normalized novelty + analyst injection
CRITICAL bug found during the B4 review (candidate c0003: train metrics
bit-identical to the seed while bank_dist read 7.04e-4): the train template's
archive loop `for _s, _r, _f, _bd in sorted(_LOG...)` rebound `_bd`, so the
"boundary" reported to the host was the collect_top-th (8th) best logged eval,
NOT the returned one — since the stellar_p2 build, val/public/private scores,
bank_dist/novelty_penalty/submittable, and best.py boundary identity mixed two
different boundaries for every candidate. Fixed by renaming the loop variables;
archive.jsonl rows were always self-consistent and stay trustworthy. All
pre-fix boundary-level verdicts (bit-identical val ties, "escapes collapse at
val", B2 c0006r1's infeasibility) are marked suspect in the wiki
(implementation-insights/boundary-report-leak.md) pending re-adjudication from
the archive. Second fix: bank_dist (container fm.bank_dist + host
bank_distance + submit_export guard, now deduped to one implementation) is
scale-normalized — each boundary divided by its own R0 before the max-coeff
diff — because every official metric is dimensionless, so a uniform rescale is
physics-null yet previously counted as novelty distance (verified: ×1.002 copy
of bank #4 = raw 2e-3, normalized ~1e-17; the export guard was gameable by pure
scaling). Escape distances measured pre-fix must be re-measured. Third (user
request, compute headroom on GLM + Claude subscription): analyst upgrade —
Config analyst_web (WebSearch/WebFetch in the in-run Fable session) and
analyst_inject N (the analyst may WRITE up to N complete candidate modules to
<run_dir>/INJECT/, consumed next generation, evaluated + gated like writer
candidates, ids c<gen>i<k>, tools locked to Write(INJECT/**)); DAG campaign
runs with analyst_web=true, analyst_inject=3, and STALL 25→40 (let branches
develop further). B4's leak-tainted run stellar_p2-s103-71917443 stopped early
and the branch restarted fresh under the fixed harness.

**2026-07-25 (Claude-role redesign, user spec — everything on the newest Opus).**
All three headless Claude roles now run claude-opus-4-8 (refiner already did;
analyst and post-branch session switched from Fable). Refiner restructured from
per-candidate to WINDOWED: every `refiner_every` (5) writer candidates one
session sees the whole window — idea/prediction/reasoning/eval feedback/code per
candidate — plus the wiki (Read/Glob/Grep, cwd = memory/<task>/, no web), and
synthesizes exactly ONE best-of-window v2 (id c<gen>f, refined_from lists the
window), evaluated + gated like any candidate. ~5x fewer refiner calls. Analyst
restructured to a decision agent: every 12 candidates (10 GLM + ~2 refiner) it
reviews the recent window AND the on-disk history (Read/Glob/Grep over
runs/*/ledger.jsonl + the wiki), decides continue | revive | pivot, may use
WebSearch/WebFetch (timeout raised to 1800s — novelty over speed), and injects
exactly ONE candidate (analyst_inject 3→1) that the next writers optimize on
top of; its wiki page gains mandatory "Decision" and "Decision log" sections so
tries/decisions stay traceable. Config gains refiner_every (default 5).

**2026-07-27 (all roles → z.ai, user directive: "we now only use z.ai models with
the z.ai API key"; no Claude-billed work).** The three headless `claude -p`
sessions are gone. Refiner (`axes/refiner.py`, class `Refiner`, alias
`ClaudeRefiner` kept for saved configs) and analyst (`axes/analyst.py`, class
`Analyst`) now call the existing z.ai client via `LLM.chat`, with the wiki's own
`read_memory`/`grep_memory` tools standing in for the old Read/Glob/Grep session
and `axes/research.py`'s `web_search`/`fetch_url` standing in for WebSearch/
WebFetch when `analyst_web` is set. Consequence to remember: refiner/analyst calls
were previously off the z.ai bill and uncharged; they are now metered through the
run's BudgetGuard, so BRANCH_USD/GLOBAL_USD cover the whole campaign and a branch
will reach its cap sooner than the old numbers imply. Analyst injection is now
parsed HOST-SIDE out of the reply's fenced code blocks instead of written by a
model file-write tool — in branch B4 every model-side INJECT write was
permission-blocked, so 8 analyst sessions produced 0 candidates. The post-branch
session (`experiments/dag_campaign.py`) became a single z.ai call over a
host-extracted candidate history + the wiki, writing only `runs/dag/proposals/
<branch>.md`; it no longer edits the wiki. That capability is deliberately dropped,
not ported: it repeatedly deleted pages without merging them (s37 index drop, s17
margin-aware-polish deletion, B2/B3 tombstones). Wiki upkeep is now the in-run
reviewer's job plus manual passes.

**2026-07-27 (result audit — what the 0.6400 is and is not).** Best candidate
`stellar_p2-s105-26196944` c0045 re-verified independently from the archive in a
clean container: official P2 **0.6400**, feasibility 0.00931 — exported to
`experiments/submissions/p2-3-dac057eed0c1b2a8-0.6400.json`. Runner-up c0088r1
re-verified at 0.6398/0.00959. Both clear the 1e-3 export guard (2.6e-3). Two
qualifications are now recorded in NOTICE.md, the wiki index and two new
performance-analysis pages, and must accompany any claim about this result:
(1) TOLERANCE. `constellaration==0.2.6` accepts any design whose worst normalized
constraint violation is ≤ `_DEFAULT_RELATIVE_TOLERANCE = 0.01`; score (L/20) is
independent of how much of that is spent. Aspect ratio is always our binding
constraint. We sit at 93 % of tolerance (A = 10.093); leaderboard #1 davidkh sits
at 7.5 % (A = 10.0075). Measured exchange rate ≈ 0.92 score per unit feasibility
(paired official evals 0.92; 307-boundary archive regression 0.91; low-margin
evidence s17 c0005f 0.6335 @ 0.00144). Normalized to his margin we score ≈0.632 —
BELOW him. The margin is exact and reproducible (feasibility is pure geometry,
fidelity-invariant to 1e-15), so there is no numerical risk, only version-drift
risk: a >0.7 %-of-bound change in the aspect-ratio definition or tolerance flips us
to 0.0, as it did for B2 c0006r1 at 0.0106.
(2) PROVENANCE. The winner is bank seed #0 (davidkh's published entry) perturbed by
‖Δ‖/‖seed‖ = 0.47 %, cosine 0.999989 — nearer to him than to the lhhhhappy seed the
wiki credited. Runs predating the seed bank (s42, s7, s11) never produced a
feasible boundary at all: official 0.0, violations 42-52× tolerance.
B6-nae-independent, the only branch that would test unaided discovery, has never
run. Actions taken: `submit_export.py` now records nearest-seed identity, cosine,
relative norm, tolerance-used fraction and an equal-margin score estimate in every
submission's provenance block, and the wiki tracks a second board — best official
score at feasibility ≤ 0.002 — on which we hold 0.6335 and have not beaten 0.6361.

**2026-07-27 (three fixes from the search audit).** (1) EVAL DEADLINE. The
in-container `_pool_eval` limit was `eval_timeout * ceil(n/workers) + 15`, which at
eval_timeout=180 and a 15-boundary batch permits ~24 min — far past the host
timeout of `cpu_budget + _SLACK`. A batch submitted near the deadline therefore
killed the whole candidate: -inf, no information, ~12 min of wall clock gone. That
was 37 of 456 campaign candidates and 30% of all eval wall-clock. The limit is now
clamped to the wall-clock actually remaining (`cpu_budget + _GRACE(60) - elapsed`),
so a late batch returns a partial result instead of losing the run.
(2) MARGIN-DISCOUNTED FITNESS. Train/val fitness is now
`p2 - 0.92*max(0, feasibility - 0.002)` (`_margin_shape`, tunable via
`STELLAR_MARGIN`), with `honest_score` computed in-container on every metrics dict
so candidates can select on it directly, and reported (never subtracted) on the
private split alongside `tolerance_used`. Rationale in
memory/stellar_p2/performance-analysis/feasibility-tolerance-economics.md: the
official rule accepts any violation ≤1% and pays ~0.92 score per unit of it, so raw
score rewarded walking aspect ratio into the wall — the previous campaign's entire
margin over the leaderboard bar came from that, not from better structure.
Validated on the champion program: train fitness 0.62688 → 0.62016 (penalty
0.006721 for feasibility 0.00931), 193 s, no timeout.
(3) SEED-BANK BLACKOUT. `STELLAR_NO_BANK=1` empties the bank process-wide, so
B6-nae-independent actually tests unaided discovery instead of asking writers
politely not to call `fm.seed_bank(i)`.
Design follow-on written to `.md/Meta-Optimizer-Design.md`: bi-level loop with
meta-fitness = hypervolume of the (honest_score, novelty-distance) Pareto front
under a fixed dollar budget, run on the CHEAP task suite with stellar_p2 held out
as the transfer test.
